package assignment;

public class AreaCalculator {

	public double AreaCalculator(double Side) {
		return side*side;
	}
	public double AreaCalculator(double length, double breadth) {
		return length*breadth;
	}
	public static float AreaCalculator(float radius) {
		return Math.pi*radius*radius;
	}
}
